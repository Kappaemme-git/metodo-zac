{
  "name": "metodo-zac-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev-server.mjs",
    "seed": "node scripts/seed-demo.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node scripts/check-site.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.3"
  }
}
